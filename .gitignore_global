# touch ~/.gitignore_global
# run `git config --global core.excludesfile ~/.gitignore_global`

# OS generated files #
######################
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
